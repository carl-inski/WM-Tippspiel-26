name: Refresh scorers

# Aktualisiert data/scorers.json (Torschützenliste der Web-App) alle ~10 Min.
# Quellen: fussballdaten.de (Führende) + football-data.org via Worker (kompletter
# Rest). Beide frei & schlüssellos – KEIN Secret nötig.
# Hinweis: GitHubs Cron ist "best effort" und kann sich verzögern. Neue Tore
# erscheinen aber ohnehin in Echtzeit über die Live-Daten (Worker); diese Action
# hält nur die kuratierte Voll-Liste (volle Namen) frisch.

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: refresh-scorers
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Torschützenliste bauen
        run: node tools/build-scorers.js
      - name: Bei Änderung committen
        run: |
          if ! git diff --quiet -- data/scorers.json; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add data/scorers.json
            git commit -m "data: refresh scorers (fussballdaten + football-data)"
            git push
          else
            echo "Keine Änderung."
          fi
